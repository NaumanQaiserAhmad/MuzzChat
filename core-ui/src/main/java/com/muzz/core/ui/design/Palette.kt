package com.muzz.core.ui.design

import androidx.compose.ui.graphics.Color

/**
 * App-level color tokens (beyond Material baseline).
 * Keep raw hex here; use them from feature modules.
 */
object Palette {
    // Chat-specific tokens (neutral bubble)
    val BubbleOtherBg = Color(0xFFE9EEF5)
    val BubbleOtherFg = Color(0xFF334155)

    // Misc UI
    val DateChipBg   = Color(0xFFE9EEF5)
    val DividerWeak  = Color(0x11000000)
}
